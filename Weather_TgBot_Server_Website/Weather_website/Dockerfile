# Вказуємо базовий образ, який містить Node.js
FROM node:14

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо package.json та package-lock.json у робочу директорію
COPY package*.json ./

# Встановлюємо залежності проекту
RUN npm install

# Копіюємо всі файли проекту в робочу директорію
COPY . .

# Вказуємо порт, який буде слухати додаток
EXPOSE 80

# Вказуємо команду для запуску додатку
CMD ["node", "index.js"]